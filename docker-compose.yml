services:
  plant-dashboard:
    build: .
    container_name: plant-dashboard
    ports:
      - "8080:8080"
    volumes:
      - plant-dashboard-data:/app/data
    restart: unless-stopped

volumes:
  plant-dashboard-data:
